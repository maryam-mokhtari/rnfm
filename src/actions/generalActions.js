export const changeModalState = (modalState) => {
  return {
    type: 'MODAL_STATE',
    modalState,
  }
}

export const changeModalText = (modalText) => {
  return {
    type: 'MODAL_TEXT',
    modalState,
  }
}
